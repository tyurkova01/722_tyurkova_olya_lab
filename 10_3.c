#include"stdio.h"
#include"math.h"
void main()
{
	int n;
	printf("n = ");
	scanf_s("%d", &n);
	if (n/10!=0)
	{
		if (n / 100 == 0);
		{
			if (n%2==0)
			{
				printf("true");
			}
			else { printf("false"); }

		}

	}

}